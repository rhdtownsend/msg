*) Currently, axis does this:
 - get_n -- number of points in axis
 - get_x_min - get x(1)
 - get_x_max - get x(n)
 - get_label - get the label
 - fetch - get the i'th x value (1 <= i <= n); throw stat error if i is out of bounds
 - locate - given x, find i such that x(i) <= x < x(i+1). Uses forum_m's locate without the right argument
 - read
 - write

*) Modifications:
 - locate returns i and u(x)
 - the function v(x) = i+u(x) is a smooth, monotonic interpolant for the function (x,i)
 - u(x) = 0 when x = x(i)
 - u(x) = 1- epsilon when x = x(i+1) - epsilon
 - how will this work?
   - given i, run locate as usual
   - use interpolating function to evaluate v(x)
   - u(x) = v(x) - i
 - interpolating function should be what?
   - linear
   - cubic hermite with monotonic
 - so, step (i)
   - split off hermite basis functions
   - calculate constructor to evaluate dx/di (Steffen)
   - perfor hermite evaluation
   
